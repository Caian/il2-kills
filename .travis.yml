os: windows
language: shell

install: 
  - choco install python3
  
before_script:
  - py -m pip install pyinstaller
  
script:
  - echo Hello
  - pyinstaller --onefile il2-kills.py
  - cd dist
  - dir
  
